// vuex computed
// 数据从 indexModules.js 中获取，需要引入该文件
import indexModules from './modules/indexModules'

const getters = {
  // 操作的是count
  // isEvenOrOdd(state){
  // return msg == "" ? "无故障" : "无报警"
  // }
}
export default getters
